import { useCallback, useEffect, useState } from "react";
import { Link, useSearchParams } from "react-router-dom";
import { apiRequest } from "../api/client.js";
import { downloadCsv } from "../api/download.js";
import { Alert } from "../components/Alert.jsx";
import { Button } from "../components/Button.jsx";
import { Card } from "../components/Card.jsx";
import { EmptyState } from "../components/EmptyState.jsx";
import { Input } from "../components/Input.jsx";
import { PageHeader } from "../components/PageHeader.jsx";
import { Select } from "../components/Select.jsx";
import { Spinner } from "../components/Spinner.jsx";

const labels = { opening_balance: "Saldo inicial", entry: "Entrada", exit: "Salida", adjustment: "Ajuste", transfer_out: "Transferencia — salida", transfer_in: "Transferencia — entrada" };
const initialForm = { search: "", startDate: "", endDate: "", productId: "", locationId: "", userId: "", categoryId: "", movementType: "" };

export function MovementReportPage({ title = "Movimientos", description = "Historial global del inventario por ubicación." }) {
  const [params, setParams] = useSearchParams();
  const [data, setData] = useState(null);
  const [error, setError] = useState(null);
  const [loading, setLoading] = useState(true);
  const [downloading, setDownloading] = useState(false);
  const [form, setForm] = useState(initialForm);
  const query = params.toString();
  const load = useCallback(async () => { setLoading(true); setError(null); try { setData(await apiRequest(`/reports/movements${query ? `?${query}` : ""}`)); } catch (requestError) { setError(requestError); } finally { setLoading(false); } }, [query]);
  useEffect(() => { load(); }, [load]);
  useEffect(() => setForm(Object.fromEntries(Object.keys(initialForm).map((key) => [key, params.get(key) ?? ""]))), [query]);
  const update = (event) => setForm((current) => ({ ...current, [event.target.name]: event.target.value }));
  const submit = (event) => { event.preventDefault(); const next = new URLSearchParams(); Object.entries(form).forEach(([key, value]) => value.trim() && next.set(key, value.trim())); next.set("page", "1"); setParams(next); };
  const clear = () => setParams({});
  const go = (page) => { const next = new URLSearchParams(params); next.set("page", page); setParams(next); };
  const download = async () => { if (downloading) return; setDownloading(true); setError(null); try { const next = new URLSearchParams(params); next.delete("page"); await downloadCsv(`/reports/movements.csv${next.toString() ? `?${next}` : ""}`, "movimientos.csv"); } catch (requestError) { setError(requestError); } finally { setDownloading(false); } };
  return <><PageHeader title={title} description={description} actions={<Button onClick={download} disabled={downloading}>{downloading ? "Exportando…" : "Exportar CSV"}</Button>} />{loading && <Spinner label="Cargando reporte" />}{error && <Alert>{error.message}<Button onClick={load}>Reintentar</Button></Alert>}{data && <><Card><form className="product-filters movement-filters" onSubmit={submit}><Input id="move-search" name="search" label="Buscar producto, SKU, código, usuario…" value={form.search} onChange={update} /><Select id="move-product" name="productId" label="Producto" value={form.productId} onChange={update}><option value="">Todos los productos</option>{(data.products ?? []).map((product) => <option key={product.id} value={product.id}>{product.name} · {product.sku}</option>)}</Select><Select id="move-category" name="categoryId" label="Categoría" value={form.categoryId} onChange={update}><option value="">Todas</option>{(data.categories ?? []).map((category) => <option key={category.id} value={category.id}>{category.name}</option>)}</Select><Input id="move-from" name="startDate" label="Fecha inicial" type="date" value={form.startDate} onChange={update} /><Input id="move-to" name="endDate" label="Fecha final" type="date" value={form.endDate} onChange={update} /><Select id="move-location" name="locationId" label="Ubicación" value={form.locationId} onChange={update}><option value="">Todas</option>{data.locations.map((location) => <option key={location.id} value={location.id}>{location.name} ({location.code})</option>)}</Select><Select id="move-user" name="userId" label="Usuario" value={form.userId} onChange={update}><option value="">Todos</option>{data.users.map((user) => <option key={user.id} value={user.id}>{user.username}</option>)}</Select><Select id="move-type" name="movementType" label="Tipo" value={form.movementType} onChange={update}><option value="">Todos</option>{Object.entries(labels).map(([key, label]) => <option key={key} value={key}>{label}</option>)}</Select><div className="product-filter-actions"><Button type="submit">Filtrar</Button><Button type="button" variant="secondary" onClick={clear}>Limpiar filtros</Button></div></form></Card><p className="muted">{data.pagination.totalItems} resultado(s).</p>{data.movements.length === 0 ? <EmptyState title={form.productId ? "No hay movimientos para este producto con los filtros seleccionados." : data.pagination.totalItems === 0 ? "Sin movimientos" : "Sin coincidencias"} description="Ajusta los filtros de búsqueda." /> : <section className="category-api-grid">{data.movements.map((movement) => <Card key={movement.id} className="category-api-card"><div className="movement-card__title"><strong>{labels[movement.type] ?? movement.type}</strong><time dateTime={movement.createdAt}>{new Intl.DateTimeFormat("es-MX", { dateStyle: "medium", timeStyle: "short" }).format(new Date(movement.createdAt))}</time></div><p><Link className="text-link" to={movement.product.status === "archived" ? `/app/products/${movement.product.id}/archived` : `/app/products/${movement.product.id}`}>{movement.product.name}</Link></p><dl><div><dt>SKU</dt><dd>{movement.product.sku}</dd></div><div><dt>Código de barras</dt><dd>{movement.barcode || "—"}</dd></div><div><dt>Categoría</dt><dd>{movement.categoryName || "—"}</dd></div><div><dt>Ubicación</dt><dd>{movement.location.name} ({movement.location.code})</dd></div><div><dt>Usuario</dt><dd>{movement.createdBy.username}</dd></div><div><dt>Cambio</dt><dd>{movement.quantityDelta > 0 ? "+" : ""}{movement.quantityDelta}</dd></div><div><dt>Saldo</dt><dd>{movement.previousStock} → {movement.resultingStock}</dd></div></dl><p>{movement.reason}{movement.reference && ` · ${movement.reference}`}</p>{movement.transferId !== null && <Link className="text-link" to={`/app/transfers/${movement.transferId}`}>Transferencia #{movement.transferId}</Link>}</Card>)}</section>}{data.pagination.totalPages > 1 && <nav className="product-pagination"><Button disabled={data.pagination.page === 1} onClick={() => go(data.pagination.page - 1)}>Anterior</Button><span aria-current="page">Página {data.pagination.page} de {data.pagination.totalPages}</span><Button disabled={data.pagination.page === data.pagination.totalPages} onClick={() => go(data.pagination.page + 1)}>Siguiente</Button></nav>}</>}</>;
}
