import{Router}from'express';import{requireAuth,requireActiveBusiness,requireBusinessRole}from'../middleware/authMiddleware.js';import{dashboard,inventory,movements}from'../controllers/reportsController.js';const r=Router();r.use(requireAuth,requireActiveBusiness,requireBusinessRole('owner','manager','viewer'));r.get('/',dashboard);r.get('/inventory',inventory);r.get('/movements',movements);export default r;
