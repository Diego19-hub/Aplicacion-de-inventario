{
  "name": "aplicacion_inventario",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node app.js",
    "dev": "node --watch app.js",
    "dev:client": "npm --prefix client run dev",
    "client:install": "npm --prefix client install",
    "client:build": "npm --prefix client run build",
    "client:build:tauri": "npm --prefix client run build:tauri",
    "build": "npm run client:build",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "db:seed": "node db/populatedb.js",
    "db:seed:dashboard": "node scripts/seedDashboardDemo.js",
    "db:bootstrap": "node scripts/bootstrapDatabase.js",
    "db:migrations:status": "node scripts/migrations.js status",
    "db:migrations:baseline": "node scripts/migrations.js baseline",
    "db:migrations:up": "node scripts/migrations.js up",
    "test": "node --test",
    "test:db:setup": "node scripts/testDatabase.js setup",
    "test:db:teardown": "node scripts/testDatabase.js teardown"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "connect-pg-simple": "^10.0.0",
    "csrf-sync": "^4.2.1",
    "dotenv": "^17.4.2",
    "exceljs": "^4.4.0",
    "express": "^5.2.1",
    "express-rate-limit": "^8.6.1",
    "express-session": "^1.19.0",
    "express-validator": "^7.3.2",
    "helmet": "^8.3.0",
    "multer": "^2.2.0",
    "openid-client": "^6.8.7",
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4",
    "supertest": "^7.2.2"
  }
}
